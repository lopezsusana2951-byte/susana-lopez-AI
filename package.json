{
  "name": "susanalopez-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Susana Lopez AI Video — static site. Homepage is prerendered to real HTML for SEO via `npm run prerender`.",
  "scripts": {
    "prerender": "node prerender.mjs"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
